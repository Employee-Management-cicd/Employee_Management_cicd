version: '3.8'

services:
  app:
    build: .
    container_name: employee-app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - FLASK_APP=run.py
    restart: always